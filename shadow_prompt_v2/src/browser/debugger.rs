// Launch Chrome / Edge incognito with --remote-debugging-port=9222.
// Detached process, temp profile path tagged with PROFILE_MARKER.

pub fn launch_incognito() -> anyhow::Result<()> {
    Ok(())
}

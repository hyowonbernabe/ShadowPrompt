// OCR query: 2-click region select → screen capture → vision LLM → clipboard + overlay.

use super::ActionContext;

pub async fn execute(_ctx: ActionContext) -> anyhow::Result<()> {
    // TODO: enter region selection mode, capture region, send to vision model.
    Ok(())
}
